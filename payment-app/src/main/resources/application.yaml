spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: user
    url: jdbc:postgresql://localhost:9002/db
    username: user

server:
  port: 8082

app:
  config:
    webhook-url: /v1/webhook